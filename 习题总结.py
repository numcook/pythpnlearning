#异或运算性质：满足交换律、结合律，任何数与0异或结果是其本身，任何数与自身异或结果是0，
#             本质上与二进制的加法运算相同，只是不进位

#lstrip()去掉左侧空格，rstrip()去掉右侧空格，strip()去掉两侧空格
#split()根据指定字符分割字符串，返回列表，splitlines()根据换行符分割字符串，返回列表

#reversed()函数返回一个反转的迭代器，使用next()函数获取值，或者使用for循环遍历
#enumerate()函数返回一个枚举对象，使用next()函数获取值，或者使用for循环遍历
